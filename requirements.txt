# Best Project: OCR Fusion + Text LLM Pipeline
# ==============================================

# PDF processing
docling
pdf2image
pymupdf

# OCR
easyocr
opencv-python
Pillow

# Surya (Marker's OCR) - use --ocr-backend surya; often better accuracy than EasyOCR
surya-ocr

# Image processing (Pillow already above)

# LLM client
requests

# JSON repair for malformed LLM output
json_repair

# Logging
rich

# Optional: LangGraph for extraction pipeline (parallel OCR node + extract node)
langgraph

# Deep learning (needed by docling/easyocr)
torch
