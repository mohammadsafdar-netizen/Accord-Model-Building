# Best Project: OCR Fusion + Text LLM Pipeline (best FOSS tools per step)
# =========================================================================

# PDF → images (primary: fast, no poppler)
pymupdf
# Fallback when pymupdf not used
pdf2image

# Document structure OCR
docling

# Bbox OCR (default: best accuracy)
surya-ocr
# Alternatives
easyocr
# paddleocr  # optional: pip install paddleocr paddlepaddle

# Image processing
opencv-python
Pillow

# Image processing (Pillow already above)

# LLM client
requests

# JSON repair for malformed LLM output
json_repair

# Logging
rich

# Optional: LangGraph for extraction pipeline (parallel OCR node + extract node)
langgraph

# Deep learning (needed by docling/easyocr)
torch
