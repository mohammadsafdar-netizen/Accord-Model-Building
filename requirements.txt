# Best Project: OCR Fusion + Text LLM Pipeline
# ==============================================

# PDF processing
docling
pdf2image
pymupdf

# OCR
easyocr
opencv-python
Pillow

# Optional: Surya (Marker's OCR engine) - often better accuracy than EasyOCR
# pip install surya-ocr  then use --ocr-backend surya
# surya-ocr

# Image processing
Pillow

# LLM client
requests

# JSON repair for malformed LLM output
json_repair

# Logging
rich

# Optional: LangGraph for extraction pipeline (parallel OCR node + extract node)
langgraph

# Deep learning (needed by docling/easyocr)
torch
